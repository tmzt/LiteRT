[package]
name        = "xtask"
description = "Workspace automation (regenerate bindings, cross-target builds)."
publish     = false
version     = "0.0.0"
edition.workspace      = true
rust-version.workspace = true

[[bin]]
name = "xtask"
path = "src/main.rs"
